/**
 * Database schema.
 *
 * Canonical tables (users, catalog, listings, orders, etc.) will be added
 * in later steps. This file is intentionally empty so the app can run
 * without a database.
 */

export {};
